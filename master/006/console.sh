# we need to install additional dependencies related to MobX
npm i mobx mobx-react --save
# also we'll need a decorators for a better looking code
npm i babel-plugin-transform-decorators-legacy --save-dev
